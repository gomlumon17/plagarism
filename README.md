# Plagiarism Detection System

## Features
- Compare two documents (txt/pdf/docx)
- Detect copied + paraphrased content
- Uses:
  - TF-IDF
  - Jaccard similarity
  - Sentence embeddings

## Run

```bash
pip install -r requirements.txt
streamlit run app.py